export default class Address {
    name: string;
    street: string;
    city: string;
    state: string;
    zip: string;
};

export interface IAddress {
    name: string;
    street: string;
    city: string;
    state: string;
    zip: string;
}